export const { REACT_APP_CLIENT_ID: CLIENT_ID } = process.env;
export const { REACT_APP_CLIENT_SECRET } = process.env;
export const { SKIP_PREFLIGHT_CHECK } = process.env;
export const { REACT_APP_BASE_URL: BASE_URL } = process.env;
